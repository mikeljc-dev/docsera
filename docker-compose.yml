# db + server levantan el backend completo con un solo comando (`pnpm
# compose:up` o `docker compose up -d --build`). El server sirve tambien
# el widget y el dashboard como estaticos.

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: docsera-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: docsera
      POSTGRES_PASSWORD: docsera
      POSTGRES_DB: docsera
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U docsera"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    container_name: docsera-server
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Dentro de la red de Docker, "localhost" es el propio contenedor: el
      # resto de valores de .env (pensados para `pnpm dev` en el host) no
      # sirven aqui, hay que sobreescribirlos apuntando a los nombres de
      # servicio / al host real.
      DATABASE_URL: postgresql://docsera:docsera@db:5432/docsera
      OLLAMA_BASE_URL: http://host.docker.internal:11434
    extra_hosts:
      # host.docker.internal solo existe por defecto en Docker Desktop
      # (Mac/Windows); esto lo hace funcionar tambien en Linux.
      - "host.docker.internal:host-gateway"
    ports:
      - "${PORT:-3000}:3000"
    depends_on:
      db:
        condition: service_healthy
